export enum Choice {
  ROCK = 'ROCK',
  PAPER = 'PAPER',
  SCISSORS = 'SCISSORS',
}

export enum ScoreStatus {
  ONGOING = 'ONGOING',
  COMPLETED = 'COMPLETED',
} 

export enum PlayResult {
  WIN = 'WIN',
  TIE = 'TIE',
  LOSE = 'LOSE',
}

export enum OrderByDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}